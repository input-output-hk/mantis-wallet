import {version, compatibleDataDirVersions} from '../../package.json'

export const MANTIS_WALLET_VERSION = `v${version}`

export const DATADIR_VERSION = version
export const COMPATIBLE_VERSIONS = compatibleDataDirVersions
